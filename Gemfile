# Gemfile (GitHub Pages compatible)
source "https://rubygems.org"

# Use the Pages-approved bundle (locks Jekyll + supported plugins)
gem "github-pages", group: :jekyll_plugins

# Needed only for local preview on Ruby 3
gem "webrick"
